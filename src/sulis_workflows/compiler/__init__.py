"""Compiler — canonical Workflow entities → LangGraph StateGraph.

The most-reusable piece (audited CLEAN-EXTRACT: imports only langgraph + stdlib +
domain + ports). Populated by the platform-engine extraction (v0.1.0). Per DR-040.
"""
