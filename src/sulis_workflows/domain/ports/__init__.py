"""Ports — the Protocol interfaces adapters implement (LLM, ContentStorage,
ToolDispatch, Checkpointing, Observability, ExecutionRuntime). Adapters live in the
consumers, injected here — never in this library. Populated at v0.1.0. Per DR-040.
"""
