api_version: sulis.workflows/v1
kind: TOOL
id: converge-confidence
version: 1.0.0
title: Converge on a confidence level
inputs:
  insights: { type: "list<profile:insight@1>" }
output:
  confidence: { type: "enum[GROUNDED, PARTIAL, INSUFFICIENT]" }
controls: [ { conventions: output-present@1 } ]
mechanism: { kind: CODE, ref: "appendix_a_corpus.tools:converge_confidence" }
effect: QUERY
